bin/
obj/
.vs/
.idea/
*.user

# Локальная БД состояния и очереди
*.db
*.db-shm
*.db-wal

# Локальные настройки и всё, что может содержать секреты
appsettings.*.Local.json
*.local.json
.env

# watchlist.json в репозитории лежит осознанно (это стартовый набор бордов),
# но приложение его перезаписывает при добавлении компаний через бота —
# после запуска возможны локальные диффы, это нормально.
